# GestureVoiceHCI — Python Dependencies
# Install with: pip install -r requirements.txt
# Python 3.11.x required

# ── Computer Vision ───────────────────────────────────────────────────────────
opencv-python==4.8.1.78
mediapipe==0.10.9

# ── GUI Automation ────────────────────────────────────────────────────────────
pyautogui==0.9.54

# ── Speech Recognition & TTS ─────────────────────────────────────────────────
SpeechRecognition==3.10.1
gTTS==2.5.1
playsound==1.3.0
PyAudio==0.2.14

# ── AI Backend ────────────────────────────────────────────────────────────────
openai==1.30.1

# ── Web UI ────────────────────────────────────────────────────────────────────
eel==0.14.0

# ── Utilities ─────────────────────────────────────────────────────────────────
numpy==1.26.4

# ── Notes ─────────────────────────────────────────────────────────────────────
# PyAudio on Windows may require a pre-built wheel:
#   pip install pipwin && pipwin install pyaudio
#
# playsound 1.3.0 is the last version compatible with Python 3.11 on Windows.
# If you encounter issues, try:
#   pip install playsound==1.2.2
#
# Set your OpenAI API key as an environment variable before running:
#   set OPENAI_API_KEY=your-key-here   (Windows CMD)
#   $env:OPENAI_API_KEY="your-key"     (PowerShell)
